# @openfn/language-fhir-ona

## 1.0.0

Implemented the `fhir-ona` adaptor with `create()`, `read()`, `delete()`, `update()` and `request()` methods.

Exported `fhir-4` builder helper functions for the fhir resources

