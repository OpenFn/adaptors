{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
    "baseUrl": {
      "title": "Base URL",
      "type": "string",
      "description": "The base URL of the OpenSPP instance, without the API path",
      "format": "uri",
      "minLength": 1,
      "examples": ["https://openspp.example.org"]
    },
    "clientId": {
      "title": "Client ID",
      "type": "string",
      "description": "OAuth client ID of an OpenSPP API client",
      "minLength": 1,
      "examples": ["client_AbC123dEf456"]
    },
    "clientSecret": {
      "title": "Client Secret",
      "type": "string",
      "description": "OAuth client secret of an OpenSPP API client",
      "writeOnly": true,
      "minLength": 1,
      "examples": ["@some(!)Str0ngp4ss0w0rd"]
    }
  },
  "type": "object",
  "additionalProperties": true,
  "required": ["baseUrl", "clientId", "clientSecret"]
}
