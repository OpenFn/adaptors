/**
 * A test operation
 * @param {number} x - the input value
 * @param {number} y - the other input value
 * @magic y - $[*]
 * @returns {number}
 */
export const operation = function (x, y) {
  return x * y;
};
