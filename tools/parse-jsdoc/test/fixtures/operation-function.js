/**
 * A test operation
 * @param {number} x - the input value
 * @param {number} y - the other input value
 * @magic y - $[*]
 * @returns {number}
 */
export function operation(x, y) {
  return x * y;
}
