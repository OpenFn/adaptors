import { expect } from 'chai';

// TODO update the unit tests here and make sure that some basic tests work
// The idea is that a generated adaptor should have tests passing right away
// A safe bet is probably building a generic patient or some datatypes
// Alternatively, we could hard-code the template with patient
