---
'@openfn/language-gmail': minor
---

Added a generic `request` function for making raw, authenticated calls against
any Gmail API endpoint (e.g. labels, threads, drafts, or fetching a single
message by id).
